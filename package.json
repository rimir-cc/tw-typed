{
    "name": "tw-typed",
    "private": true,
    "description": "Tests for the rimir/typed TiddlyWiki plugin",
    "scripts": {
        "test": "TIDDLYWIKI_CLI_MODE=1 TIDDLYWIKI_PLUGIN_PATH=../.. npx tiddlywiki test-edition --test"
    }
}
